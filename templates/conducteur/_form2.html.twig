<h5 class="text-center">Saisissez les données du conducteur:</h5>
{{ form_start(form) }}


    <div class="form-group">

        {{ form_row(form.prenom) }}

    </div>

    <div class="form-group">

        {{ form_row(form.nom) }}

    </div>

    <div class="form-group">

        {{ form_row(form.description) }}

    </div>

    <div class="form-group">

        {{ form_row(form.email) }}

    </div>

    <div class="form-group">
    
        {{ form_row(form.age) }}
    
    </div>

    <button class="btn btn-success">{{ button_label|default('Save') }}</button>



{{ form_end(form) }}