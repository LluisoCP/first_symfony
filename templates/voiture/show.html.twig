{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-6 offset-3">

            <h1>Voiture</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ voiture.id }}</td>
                    </tr>
                    <tr>
                        <th>Marque</th>
                        <td>{{ voiture.Marque }}</td>
                    </tr>
                    <tr>
                        <th>Modele</th>
                        <td>{{ voiture.Modele }}</td>
                    </tr>
                    <tr>
                        <th>Immatriculation</th>
                        <td>{{ voiture.Immatriculation }}</td>
                    </tr>
                    <tr>
                        <th>Couleur</th>
                        <td>{{ voiture.Couleur }}</td>
                    </tr>
                    {#<tr>
                        <th>CreatedAt</th>
                        <td>{{ voiture.createdAt ? voiture.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>#}
                </tbody>
            </table>

            <a class="btn btn-info" href="{{ path('voiture_index') }}">back to list</a>

            <a class="btn btn-success" href="{{ path('voiture_edit', {'id': voiture.id}) }}">edit</a>

            {{ include('voiture/_delete_form.html.twig') }}

        </div>
    </div>
</div>
{% endblock %}
